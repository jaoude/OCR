@{
    ViewData["Title"] = "Page";
}
<h2>Search for your page here</h2>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">

    <style>
        form.example input[type=text] {
            padding: 10px;
            font-size: 17px;
            border: 1px solid grey;
            float: left;
            width: 80%;
            background: #f1f1f1;
        }

        form.example button {
            float: left;
            width: 20%;
            padding: 10px;
            background: #2196F3;
            color: white;
            font-size: 17px;
            border: 1px solid grey;
            border-left: none;
            cursor: pointer;
        }

        form.example::after {
            content: "";
            clear: both;
            display: table;
        }

        .left {
            float: left;
            width: 75%;
            overflow: hidden;
        }

        .right {
            float: left;
            width: 25%;
            overflow: hidden;
        }

        table, th, td {
            border: 1px solid black;
        }
    </style>

    <script type="text/javascript" src="//api.yamli.com/js/yamli_api.js"></script>
        <script type="text/javascript">
            if (typeof (Yamli) == 'object' && Yamli.init()) {
                Yamli.yamlify('#myImput');
            }
        </script>


</head>
<body>

    <input type="button" value="Upload Pages" id="idUploadPages" />
    
    <form class="example">
    <input id='myInput' type='text' name="search" placeholder="Search...">
    <button id="search"><i class="fa fa-search"></i></button>
    <br />
   
    <center>
        <div class="left">
            <img id="myimage" src="@Url.Action("GetDummyImage", "Page")?id=2" />
            <nav aria-label="Page navigation example">
                <ul class="pagination">
                    <li class="page-item"><a id="previous" class="page-link" href="#">Previous</a></li>
                    <li class="page-item"><a id="next" class="page-link" href="#">Next</a></li>
                </ul>
            </nav>
        </div>
        <div id="myResultTable" class="right">
            <select id="Results" size="10"></select>
        </div>
    </center>
    </form>


</body>
</html>



<script type="text/javascript">
    $(document).ready(function () {

        $('.example').submit(function (event) {
            event.preventDefault();
            e.stopPropagation();
        });

        $('#idUploadPages').on('click', function () {
            $.ajax({
                url: "@(Url.Action("Upload", "Page"))",
                type: "POST",
                //data: null,
                cache: false,
                async: true,
                success: function (response) {
                    alert(response.responseText);
                },
                error: function (response) {
                    alert(response.responseText)
                },
            });
        });

        //$("#myInput").on("change paste keyup", function () {
        //    submitSearch();
        //});


        $('#search').on('click', function () {
            submitSearch();
        });

        $('#previous').on('click', function () {
        });

        $('#next').on('click', function () {
        });

        $("select#Results").change(function () {
            var id = $(this).children(":selected").attr('id');
            $("#myimage").attr("src", "@Url.Action("GetDummyImage", "Page")?id="+id);
        });



        function submitSearch() {
            var text = $("#myInput").val();
            $.ajax({
                type: "POST",
                url: '/Page/SearchForText?search=' + text,
                contentType: "application/json; charset=utf-8",
                cache: false,
                async: true,
                success: function (response) {
                    $('#Results').html(response.responseText);
                },
                error: function (response) {
                    alert(response.responseText);
                },
            });
        }
    });
</script>

@*Add ajax jquery call to call the pagecontroller searchfortext
    (string as parameter) retrn list of (page number, string )
    Paste the result in the div.update (listbox)
    Upon click on any reult => getImage(pagenumber)
    On next arrow and previous arrow (getImage (pagenum-1), pagenum+1)*@